const SourceData = require('./SourceData.json');
const UserData = require('./UserData.json');
// Something more

module.exports = () => ({
    SourceData: SourceData,
    UserData: UserData
  // Something more
});